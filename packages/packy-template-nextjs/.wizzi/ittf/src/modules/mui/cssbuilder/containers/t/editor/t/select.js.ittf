{
    $params id, options, defaultValue|@@null
    
    $
        var optionValues = options.split('|');

    @ id '${id}'
    @ type 'select'
    { data
        [ options
            $foreach item in optionValues
                @ '${item}'
    $if defaultValue != null
        @ defaultValue '${defaultValue}'