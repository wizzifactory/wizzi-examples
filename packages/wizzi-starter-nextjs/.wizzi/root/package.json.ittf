{
	name "${pkg.name}"
	version "${pkg.version}"
    description "${pkg.description}"
	author "${pkg.author}"
	license "${pkg.license}"
	
    { scripts
		test "echo \"Error: no test specified\" && exit 1"
    	$include v07/app/package/nextjs/scripts
	
    { dependencies
		$include v07/app/package/nextjs/dependencies

	{ devDependencies
		$include v07/app/package/nextjs/devDependencies
